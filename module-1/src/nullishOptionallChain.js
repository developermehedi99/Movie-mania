"use strict";
var _a;
{
    const authentication = undefined;
    const user = authentication !== null && authentication !== void 0 ? authentication : 'ghust';
    const chakri = {
        name: 'goverment',
        job: 'permanent'
    };
    const optionalJob = (_a = chakri === null || chakri === void 0 ? void 0 : chakri.job2) !== null && _a !== void 0 ? _a : 'this job sobai pare na';
    console.log(optionalJob);
}
